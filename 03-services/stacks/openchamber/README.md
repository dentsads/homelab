# TODOS

Things to configure:
- ../../../data/workspaces chwon to 1000 (sudo chown -R 1000:1000 data/)
- add github connection
- add github identity
- mount ~/.ssh into container for git ssh certs. add public key to github ssh keys
- ssh-keyscan github.com >> ~/.ssh/known_hosts